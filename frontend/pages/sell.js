import CreateItem from '../components/CreateItem';
import PleaseSignin from '../components/PelaseSignin';

const Sell = props => (
  <div>
    <PleaseSignin>
      <CreateItem />
    </PleaseSignin>
  </div>
)

export default Sell
